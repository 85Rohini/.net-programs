using System;

class Program
{
    static void Main()
    {
        Random random = new Random();

        Console.WriteLine("Random Numbers:");
        for (int i = 0; i < 5; i++)
        {
            Console.WriteLine(random.Next(1, 101)); // Generates numbers between 1 and 100
        }
    }
}
